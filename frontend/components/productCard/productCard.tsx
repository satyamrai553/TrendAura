

interface ProductCard{
    productImage:string, productTitle:string, productRating: string, productPrie:string
}

export default function ProductCard(props:ProductCard){
    const {productImage, productTitle, productRating, productPrie } = props;
    return(
        <>
        
        </>
    )
}